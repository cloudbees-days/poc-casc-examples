# bundle.yaml
apiVersion: "2"            # use 2 to enable plugin mgmt v2 (backward compatible)
id: "mastercard-folder-rbac"
version: "9"               # <— bump this anytime you change the bundle
description: "Create folder + RBAC and ensure CasC/Items plugins are present"
#plugins:
#  - "plugins.yaml"
rbac:
  - "rbac.yaml"
items:
  - "items.yaml"
